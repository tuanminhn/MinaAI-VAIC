[project]
name = "mina-knowledge-graph"
version = "0.1.0"
description = "PDF provenance and curated knowledge-graph pipeline for Mina AI"
requires-python = ">=3.11"

[project.scripts]
mina-kg = "mina_kg.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

